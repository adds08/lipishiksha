

// DEFAULT_GRID_ROWS and DEFAULT_GRID_COLS are kept for labeling form defaults.
export const DEFAULT_GRID_ROWS = 10;
export const DEFAULT_GRID_COLS = 10;

// Alphabet arrays and SUPPORTED_LANGUAGES are removed as this data
// will now come from the font files stored in data/fonts.json
// and accessed via font-data-service.ts.

